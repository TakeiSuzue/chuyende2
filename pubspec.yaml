name: emotion_diary_app
description: Emotion Diary App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  image_picker: ^1.0.7
  geolocator: ^10.1.0
  intl: ^0.18.1
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.2
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-ma
  terial-design: true